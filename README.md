# 🧠 YOLOv8 System Monitoring & Annotation

Проект для **детекции объектов с веб-камеры** с использованием **YOLOv8** и сохранением аннотаций в **JSON**.  
Поддерживает режим **умного (оптимизированного) сохранения**, при котором данные записываются **только при значимых изменениях в кадре**.

Подходит для:
- проектов по компьютерному зрению  
- мониторинга помещений  
- анализа поведения объектов  
- подготовки датасетов  
- учебных и исследовательских работ  

---

## 🚀 Возможности

### 🔹 Оптимизированный режим
Сохраняет кадр **только если**:
- изменилось количество объектов
- изменились классы объектов
- объект сильно сместился
- IoU между bounding box меньше заданного порога

**Горячие клавиши:**
- `S` — принудительно сохранить кадр
- `Q` — выход

---

### 🔹 Простой режим
- Сохраняет **каждый кадр**
- Все данные пишутся в один JSON-файл
- Удобно для тестирования и обучения

---

### 🔹 Быстрый тест
- Обрабатывает **50 кадров**
- Проверка корректной работы YOLO и камеры

---

## 🧰 Используемые технологии

- Python 3.9+
- OpenCV
- Ultralytics YOLOv8
- NumPy
- JSON

---

## 📦 Установка

### 1. Установка зависимостей
```bash
pip install ultralytics opencv-python numpy
```

### 2. Проверь наличие веб-камеры  
По умолчанию используется:
```python
cv2.VideoCapture(0)
```

---

## ▶️ Запуск

```bash
python script_1.py
```

После запуска выбери режим работы:

```
1. Оптимизированное сохранение (только при изменениях)
2. Простое сохранение (все кадры в один файл)
3. Быстрый тест
```

---

## 🧾 Формат выходных данных (JSON)

```json
{
  "session_info": {
    "total_processed_frames": 42,
    "total_objects": 128,
    "save_time": "2026-01-05T12:00:00"
  },
  "frames": {
    "frame_1": {
      "frame_number": 10,
      "timestamp": "...",
      "objects": {
        "person_0_10": {
          "label": "person",
          "confidence": 0.87
        }
      }
    }
  }
}
```

---

## ⚙️ Основные параметры

```python
self.position_threshold = 50
self.iou_threshold = 0.3
confidence > 0.5
```

---

## 📌 Примечание

Модель `yolov8n.pt` загружается автоматически при первом запуске.

---

## 📄 Лицензия

MIT License

---

## ✨ Автор

Михаил Провков
